/** Inner padding of dashboard cards (`ImpactOverviewCard`, etc.). */
export const tenantCardInsetClassName = "p-6 sm:p-10";

/** Page title block: horizontal inset aligned with card interiors (no extra vertical padding). */
export const tenantPageHeaderClassName = "w-full px-6 sm:px-10";

/** Shared shell classes for authenticated tenant pages. */
export const tenantPageMainClassName =
  "relative min-h-dvh w-full min-w-0 space-y-8 bg-[color-mix(in_srgb,var(--card)_92%,var(--background)_8%)] px-4 pt-6 pb-28 sm:px-6 sm:pt-10 sm:pb-28 md:px-10 md:py-10";
